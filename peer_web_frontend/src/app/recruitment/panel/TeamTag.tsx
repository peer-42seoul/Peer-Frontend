import { Box } from "@mui/material"

const TeamTag = () => {
  // 태그 를 검색하고, 있는 것중에서 기입을 해야해서 생각보다 큰 로직이 될 것같다.
  // 현지님의 태그부분 공통모듈로 빼놓으시면 가져다 사용 예정.
  return (
    <Box>
      hey
    </Box>
  )
}

export default TeamTag
