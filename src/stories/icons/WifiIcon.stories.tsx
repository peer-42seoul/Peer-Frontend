import type { Meta, StoryObj } from '@storybook/react'
  import { WifiIcon } from '@/icons'

  const meta: Meta<typeof WifiIcon> = {
    component: WifiIcon,
  }
  export default meta

  type Story = StoryObj<typeof WifiIcon>

  export const Default: Story = {}
