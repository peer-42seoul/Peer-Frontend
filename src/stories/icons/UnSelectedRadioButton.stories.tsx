import type { Meta, StoryObj } from '@storybook/react'
  import { UnSelectedRadioButton } from '@/icons'

  const meta: Meta<typeof UnSelectedRadioButton> = {
    component: UnSelectedRadioButton,
  }
  export default meta

  type Story = StoryObj<typeof UnSelectedRadioButton>

  export const Default: Story = {}
