import type { Meta, StoryObj } from '@storybook/react'
  import { TrashLineIcon } from '@/icons'

  const meta: Meta<typeof TrashLineIcon> = {
    component: TrashLineIcon,
  }
  export default meta

  type Story = StoryObj<typeof TrashLineIcon>

  export const Default: Story = {}
