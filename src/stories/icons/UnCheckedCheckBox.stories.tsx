import type { Meta, StoryObj } from '@storybook/react'
  import { UnCheckedCheckBox } from '@/icons'

  const meta: Meta<typeof UnCheckedCheckBox> = {
    component: UnCheckedCheckBox,
  }
  export default meta

  type Story = StoryObj<typeof UnCheckedCheckBox>

  export const Default: Story = {}
