import type { Meta, StoryObj } from '@storybook/react'
  import { UserCheckIcon } from '@/icons'

  const meta: Meta<typeof UserCheckIcon> = {
    component: UserCheckIcon,
  }
  export default meta

  type Story = StoryObj<typeof UserCheckIcon>

  export const Default: Story = {}
