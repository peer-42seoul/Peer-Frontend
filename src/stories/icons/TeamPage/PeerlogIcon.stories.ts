import type { Meta, StoryObj } from '@storybook/react'
    import { PeerlogIcon } from '@/icons/TeamPage'

    const meta: Meta<typeof PeerlogIcon> = {
      component: PeerlogIcon,
    }
    export default meta

    type Story = StoryObj<typeof PeerlogIcon>

    export const Default: Story = {}
