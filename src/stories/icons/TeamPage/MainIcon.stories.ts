import type { Meta, StoryObj } from '@storybook/react'
    import { MainIcon } from '@/icons/TeamPage'

    const meta: Meta<typeof MainIcon> = {
      component: MainIcon,
    }
    export default meta

    type Story = StoryObj<typeof MainIcon>

    export const Default: Story = {}
