import type { Meta, StoryObj } from '@storybook/react'
    import { BoardIcon } from '@/icons/TeamPage'

    const meta: Meta<typeof BoardIcon> = {
      component: BoardIcon,
    }
    export default meta

    type Story = StoryObj<typeof BoardIcon>

    export const Default: Story = {}
