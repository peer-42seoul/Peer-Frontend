import type { Meta, StoryObj } from '@storybook/react'
    import { ShowcaseIcon } from '@/icons/TeamPage'

    const meta: Meta<typeof ShowcaseIcon> = {
      component: ShowcaseIcon,
    }
    export default meta

    type Story = StoryObj<typeof ShowcaseIcon>

    export const Default: Story = {}
