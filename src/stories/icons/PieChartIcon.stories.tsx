import type { Meta, StoryObj } from '@storybook/react'
  import { PieChartIcon } from '@/icons'

  const meta: Meta<typeof PieChartIcon> = {
    component: PieChartIcon,
  }
  export default meta

  type Story = StoryObj<typeof PieChartIcon>

  export const Default: Story = {}
