import type { Meta, StoryObj } from '@storybook/react'
  import { CheckBoxSelected } from '@/icons'

  const meta: Meta<typeof CheckBoxSelected> = {
    component: CheckBoxSelected,
  }
  export default meta

  type Story = StoryObj<typeof CheckBoxSelected>

  export const Default: Story = {}
