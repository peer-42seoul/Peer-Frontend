import type { Meta, StoryObj } from '@storybook/react'
  import { TagIcon } from '@/icons'

  const meta: Meta<typeof TagIcon> = {
    component: TagIcon,
  }
  export default meta

  type Story = StoryObj<typeof TagIcon>

  export const Default: Story = {}
