import type { Meta, StoryObj } from '@storybook/react'
    import { HitchIcon } from '@/icons/Nav'

    const meta: Meta<typeof HitchIcon> = {
      component: HitchIcon,
    }
    export default meta

    type Story = StoryObj<typeof HitchIcon>

    export const Default: Story = {}
