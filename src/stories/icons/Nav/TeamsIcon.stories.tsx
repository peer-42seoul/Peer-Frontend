import type { Meta, StoryObj } from '@storybook/react'
    import { TeamsIcon } from '@/icons/Nav'

    const meta: Meta<typeof TeamsIcon> = {
      component: TeamsIcon,
    }
    export default meta

    type Story = StoryObj<typeof TeamsIcon>

    export const Default: Story = {}
