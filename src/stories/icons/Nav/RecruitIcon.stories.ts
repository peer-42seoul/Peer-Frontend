import type { Meta, StoryObj } from '@storybook/react'
    import { RecruitIcon } from '@/icons/Nav'

    const meta: Meta<typeof RecruitIcon> = {
      component: RecruitIcon,
    }
    export default meta

    type Story = StoryObj<typeof RecruitIcon>

    export const Default: Story = {}
