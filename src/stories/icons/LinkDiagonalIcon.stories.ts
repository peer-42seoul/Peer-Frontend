import type { Meta, StoryObj } from '@storybook/react'
  import { LinkDiagonalIcon } from '@/icons'

  const meta: Meta<typeof LinkDiagonalIcon> = {
    component: LinkDiagonalIcon,
  }
  export default meta

  type Story = StoryObj<typeof LinkDiagonalIcon>

  export const Default: Story = {}
