import type { Meta, StoryObj } from '@storybook/react'
    import { SilhouetteIcon } from '@/icons/MyPage'

    const meta: Meta<typeof SilhouetteIcon> = {
      component: SilhouetteIcon,
    }
    export default meta

    type Story = StoryObj<typeof SilhouetteIcon>

    export const Default: Story = {}
