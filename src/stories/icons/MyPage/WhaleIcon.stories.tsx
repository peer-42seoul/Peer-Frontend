import type { Meta, StoryObj } from '@storybook/react'
    import { WhaleIcon } from '@/icons/MyPage'

    const meta: Meta<typeof WhaleIcon> = {
      component: WhaleIcon,
    }
    export default meta

    type Story = StoryObj<typeof WhaleIcon>

    export const Default: Story = {}
